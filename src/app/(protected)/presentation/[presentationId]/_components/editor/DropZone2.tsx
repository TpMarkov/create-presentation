import React from "react";

type Props = {
  index: number;
  parentId: string;
  slideId: string;
};

const DropZone2 = ({ index, parentId, slideId }: Props) => {
  return <div>DropZone</div>;
};

export default DropZone2;
